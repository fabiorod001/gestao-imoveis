# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/gestao_imoveis

# Environment
NODE_ENV=development

# No Replit dependencies - 100% INDEPENDENT

# Application Settings
PORT=5173

# Security (generate new secrets for production - minimum 32 characters)
# IMPORTANT: Generate a secure random string with at least 32 characters
# Example: Use `openssl rand -base64 32` to generate
SESSION_SECRET=your-session-secret-here-minimum-32-characters-required

# Optional: External Services
# OPENID_CLIENT_ID=
# OPENID_CLIENT_SECRET=
# OPENID_ISSUER=